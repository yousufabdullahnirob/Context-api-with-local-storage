# Context-api-with-local-storage
Todo app built with React  State managed via Context API  Data persisted in browser localStorage  Supports adding, deleting, toggling todos  On load, reads todos from localStorage  Automatically syncs changes to localStorage  Clean folder structure: components, context, utils
